// ─── Queue names ─────────────────────────────────────────────────────────────

export const QUEUE_NOTIFICATIONS = 'notifications';
export const QUEUE_ANALYTICS     = 'analytics';

// ─── Job names — notifications queue ─────────────────────────────────────────

export const JOB_SEND_PUSH   = 'send-push';
export const JOB_BROADCAST   = 'broadcast';

// ─── Job names — analytics queue ─────────────────────────────────────────────

export const JOB_NIGHTLY_REPORT      = 'nightly-report';
export const JOB_RESET_DAILY_QUOTA   = 'reset-daily-quota';
export const JOB_RESET_MONTHLY_QUOTA = 'reset-monthly-quota';
