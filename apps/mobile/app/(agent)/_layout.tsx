import { Stack } from 'expo-router';

/** Placeholder agent stack — replaced by the tab navigator in stage 3. */
export default function AgentLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
